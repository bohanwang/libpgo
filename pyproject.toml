[project]
name = "libpgo"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "pymeshfix",
    "scikit-image",
    "trimesh",
]

[tool.uv]
package = false

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "libpgo"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
]

[project.optional-dependencies]
test = ["pytest>=6.0"]

[tool.uv]
package = false

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]
build-backend = "setuptools.build_meta"
